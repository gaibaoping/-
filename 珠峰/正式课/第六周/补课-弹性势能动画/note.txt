1、JS盒子盒子模型中13个属性的某些属性（offsetLeft,offsetTop...）获取到的样式值都不会出现小数，如果元素的left的值是小数，我们通过offsetLeft获取的结果会进行四舍五入

2、水平运动的速度和即将结束拖拽时候的速度有关系，即将结束的时候如果拖拽的快，那水平得速度就快，反之则慢

    浏览器都存在一定的反应时间，假设谷歌浏览器的反应时间是10ms，它是如何识别mousemove事件的呢？
    在反应时间到的时候，检测当前的位置距离上一次的位置是否发生位移改变，发生了认为是事件执行了，反之认为没有触发事件